---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "signoz_route_policy Resource - SigNoz"
subcategory: ""
description: |-
  
---

# signoz_route_policy (Resource)



## Example Usage

```terraform
# Route policies reference channels by display name.
resource "signoz_notification_channel" "oncall_slack" {
  display_name = "oncall-slack"

  config = {
    webhook = {
      kind = "webhook"
      spec = {
        url           = "https://example.com/webhook-oncall"
        send_resolved = true
      }
    }
  }
}

resource "signoz_route_policy" "critical_to_oncall" {
  name        = "route-critical-to-oncall"
  description = "Route critical payments alerts to the on-call channel."
  kind        = "policy"
  expression  = "service == \"payments\" && severity == \"critical\""
  channels    = [signoz_notification_channel.oncall_slack.display_name]
  tags        = ["team:platform"]
}
```

```terraform
resource "signoz_notification_channel" "oncall_slack" {
  display_name = "oncall-slack"

  config = {
    webhook = {
      kind = "webhook"
      spec = {
        url           = "https://example.com/webhook-oncall"
        send_resolved = true
      }
    }
  }
}

resource "signoz_route_policy" "default_kind" {
  name       = "route-critical-to-oncall"
  expression = "service == \"payments\" && severity == \"critical\""
  channels   = [signoz_notification_channel.oncall_slack.display_name]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `channels` (List of String)
- `expression` (String)
- `name` (String)

### Optional

- `description` (String)
- `kind` (String)
- `tags` (List of String)

### Read-Only

- `id` (String) The ID of this resource.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Route policies are imported using their ID.
terraform import signoz_route_policy.critical_to_oncall 0192a9c2-1234-7abc-8def-0123456789ab
```
