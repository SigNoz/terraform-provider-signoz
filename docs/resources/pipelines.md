---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "signoz_pipelines Resource - SigNoz"
subcategory: ""
description: |-
  
---

# signoz_pipelines (Resource)



## Example Usage

```terraform
# Manages the full ordered list of log pipelines. The SigNoz API saves all
# pipelines as one versioned list, so this resource is a singleton: declare
# every pipeline here, in processing order.
resource "signoz_pipelines" "all" {
  pipelines = [
    {
      name        = "parse-nginx-logs"
      alias       = "parse-nginx-logs"
      description = "Parse JSON access logs from the nginx service."

      filter = {
        items = [
          {
            key = {
              key       = "service.name"
              data_type = "string"
              type      = "resource"
            }
            op    = "="
            value = jsonencode("nginx")
          }
        ]
      }

      config = [
        {
          type       = "json_parser"
          name       = "Parse body as JSON"
          parse_from = "body"
          parse_to   = "attributes"
        },
        {
          type  = "remove"
          name  = "Drop the raw password field"
          field = "attributes.password"
        }
      ]
    }
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `pipelines` (Attributes List) (see [below for nested schema](#nestedatt--pipelines))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--pipelines"></a>
### Nested Schema for `pipelines`

Required:

- `alias` (String)
- `config` (Attributes List) (see [below for nested schema](#nestedatt--pipelines--config))
- `filter` (Attributes) (see [below for nested schema](#nestedatt--pipelines--filter))
- `name` (String)

Optional:

- `description` (String)
- `enabled` (Boolean)

<a id="nestedatt--pipelines--config"></a>
### Nested Schema for `pipelines.config`

Required:

- `type` (String)

Optional:

- `default` (String)
- `enable_flattening` (Boolean)
- `enable_paths` (Boolean)
- `enabled` (Boolean)
- `expr` (String)
- `field` (String)
- `fields` (List of String)
- `from` (String)
- `if` (String)
- `layout` (String)
- `layout_type` (String)
- `mapping` (Map of List of String)
- `name` (String)
- `on_error` (String)
- `overwrite_text` (Boolean)
- `parse_from` (String)
- `parse_to` (String)
- `path_prefix` (String)
- `pattern` (String)
- `regex` (String)
- `span_id` (Attributes) (see [below for nested schema](#nestedatt--pipelines--config--span_id))
- `to` (String)
- `trace_flags` (Attributes) (see [below for nested schema](#nestedatt--pipelines--config--trace_flags))
- `trace_id` (Attributes) (see [below for nested schema](#nestedatt--pipelines--config--trace_id))
- `value` (String)

<a id="nestedatt--pipelines--config--span_id"></a>
### Nested Schema for `pipelines.config.span_id`

Required:

- `parse_from` (String)


<a id="nestedatt--pipelines--config--trace_flags"></a>
### Nested Schema for `pipelines.config.trace_flags`

Required:

- `parse_from` (String)


<a id="nestedatt--pipelines--config--trace_id"></a>
### Nested Schema for `pipelines.config.trace_id`

Required:

- `parse_from` (String)



<a id="nestedatt--pipelines--filter"></a>
### Nested Schema for `pipelines.filter`

Required:

- `items` (Attributes List) (see [below for nested schema](#nestedatt--pipelines--filter--items))

Optional:

- `op` (String)

<a id="nestedatt--pipelines--filter--items"></a>
### Nested Schema for `pipelines.filter.items`

Required:

- `key` (Attributes) (see [below for nested schema](#nestedatt--pipelines--filter--items--key))
- `op` (String)

Optional:

- `value` (String)

<a id="nestedatt--pipelines--filter--items--key"></a>
### Nested Schema for `pipelines.filter.items.key`

Required:

- `data_type` (String)
- `key` (String)
- `type` (String)

Optional:

- `is_column` (Boolean)
- `is_json` (Boolean)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# The pipeline list is a singleton; it is always imported with the fixed ID "pipelines".
terraform import signoz_pipelines.all pipelines
```
