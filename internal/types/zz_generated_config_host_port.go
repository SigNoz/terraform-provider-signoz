// Code generated by skaff types DO NOT EDIT.

package types

import (
	"context"
	"fmt"
	"strings"

	"github.com/hashicorp/terraform-plugin-framework/attr"
	"github.com/hashicorp/terraform-plugin-framework/diag"
	"github.com/hashicorp/terraform-plugin-framework/types"
	"github.com/hashicorp/terraform-plugin-framework/types/basetypes"
	"github.com/hashicorp/terraform-plugin-go/tftypes"
)

var _ basetypes.ObjectTypable = ConfigHostPortType{}

type ConfigHostPortType struct {
	basetypes.ObjectType
}

func (t ConfigHostPortType) Equal(o attr.Type) bool {
	other, ok := o.(ConfigHostPortType)

	if !ok {
		return false
	}

	return t.ObjectType.Equal(other.ObjectType)
}

func (t ConfigHostPortType) String() string {
	return "ConfigHostPortType"
}

func (t ConfigHostPortType) ValueFromObject(ctx context.Context, in basetypes.ObjectValue) (basetypes.ObjectValuable, diag.Diagnostics) {
	var diags diag.Diagnostics

	if diags.HasError() {
		return nil, diags
	}

	return ConfigHostPortValue{
		state: attr.ValueStateKnown,
	}, diags
}

func NewConfigHostPortValueNull() ConfigHostPortValue {
	return ConfigHostPortValue{
		state: attr.ValueStateNull,
	}
}

func NewConfigHostPortValueUnknown() ConfigHostPortValue {
	return ConfigHostPortValue{
		state: attr.ValueStateUnknown,
	}
}

func NewConfigHostPortValue(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) (ConfigHostPortValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	// Reference: https://github.com/hashicorp/terraform-plugin-framework/issues/521
	ctx := context.Background()

	for name, attributeType := range attributeTypes {
		attribute, ok := attributes[name]

		if !ok {
			diags.AddError(
				"Missing ConfigHostPortValue Attribute Value",
				"While creating a ConfigHostPortValue value, a missing attribute value was detected. "+
					"A ConfigHostPortValue must contain values for all attributes, even if null or unknown. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("ConfigHostPortValue Attribute Name (%s) Expected Type: %s", name, attributeType.String()),
			)

			continue
		}

		if !attributeType.Equal(attribute.Type(ctx)) {
			diags.AddError(
				"Invalid ConfigHostPortValue Attribute Type",
				"While creating a ConfigHostPortValue value, an invalid attribute value was detected. "+
					"A ConfigHostPortValue must use a matching attribute type for the value. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("ConfigHostPortValue Attribute Name (%s) Expected Type: %s\n", name, attributeType.String())+
					fmt.Sprintf("ConfigHostPortValue Attribute Name (%s) Given Type: %s", name, attribute.Type(ctx)),
			)
		}
	}

	for name := range attributes {
		_, ok := attributeTypes[name]

		if !ok {
			diags.AddError(
				"Extra ConfigHostPortValue Attribute Value",
				"While creating a ConfigHostPortValue value, an extra attribute value was detected. "+
					"A ConfigHostPortValue must not contain values beyond the expected attribute types. "+
					"This is always an issue with the provider and should be reported to the provider developers.\n\n"+
					fmt.Sprintf("Extra ConfigHostPortValue Attribute Name: %s", name),
			)
		}
	}

	if diags.HasError() {
		return NewConfigHostPortValueUnknown(), diags
	}

	if diags.HasError() {
		return NewConfigHostPortValueUnknown(), diags
	}

	return ConfigHostPortValue{
		state: attr.ValueStateKnown,
	}, diags
}

func NewConfigHostPortValueMust(attributeTypes map[string]attr.Type, attributes map[string]attr.Value) ConfigHostPortValue {
	object, diags := NewConfigHostPortValue(attributeTypes, attributes)

	if diags.HasError() {
		// This could potentially be added to the diag package.
		diagsStrings := make([]string, 0, len(diags))

		for _, diagnostic := range diags {
			diagsStrings = append(diagsStrings, fmt.Sprintf(
				"%s | %s | %s",
				diagnostic.Severity(),
				diagnostic.Summary(),
				diagnostic.Detail()))
		}

		panic("NewConfigHostPortValueMust received error(s): " + strings.Join(diagsStrings, "\n"))
	}

	return object
}

func (t ConfigHostPortType) ValueFromTerraform(ctx context.Context, in tftypes.Value) (attr.Value, error) {
	if in.Type() == nil {
		return NewConfigHostPortValueNull(), nil
	}

	if !in.Type().Equal(t.TerraformType(ctx)) {
		return nil, fmt.Errorf("expected %s, got %s", t.TerraformType(ctx), in.Type())
	}

	if !in.IsKnown() {
		return NewConfigHostPortValueUnknown(), nil
	}

	if in.IsNull() {
		return NewConfigHostPortValueNull(), nil
	}

	attributes := map[string]attr.Value{}

	val := map[string]tftypes.Value{}

	err := in.As(&val)

	if err != nil {
		return nil, err
	}

	for k, v := range val {
		a, err := t.AttrTypes[k].ValueFromTerraform(ctx, v)

		if err != nil {
			return nil, err
		}

		attributes[k] = a
	}

	return NewConfigHostPortValueMust(ConfigHostPortValue{}.AttributeTypes(ctx), attributes), nil
}

func (t ConfigHostPortType) ValueType(ctx context.Context) attr.Value {
	return ConfigHostPortValue{}
}

var _ basetypes.ObjectValuable = ConfigHostPortValue{}

type ConfigHostPortValue struct {
	state attr.ValueState
}

func (v ConfigHostPortValue) ToTerraformValue(ctx context.Context) (tftypes.Value, error) {
	attrTypes := make(map[string]tftypes.Type, 0)

	objectType := tftypes.Object{AttributeTypes: attrTypes}

	switch v.state {
	case attr.ValueStateKnown:
		vals := make(map[string]tftypes.Value, 0)

		if err := tftypes.ValidateValue(objectType, vals); err != nil {
			return tftypes.NewValue(objectType, tftypes.UnknownValue), err
		}

		return tftypes.NewValue(objectType, vals), nil
	case attr.ValueStateNull:
		return tftypes.NewValue(objectType, nil), nil
	case attr.ValueStateUnknown:
		return tftypes.NewValue(objectType, tftypes.UnknownValue), nil
	default:
		panic(fmt.Sprintf("unhandled Object state in ToTerraformValue: %s", v.state))
	}
}

func (v ConfigHostPortValue) IsNull() bool {
	return v.state == attr.ValueStateNull
}

func (v ConfigHostPortValue) IsUnknown() bool {
	return v.state == attr.ValueStateUnknown
}

func (v ConfigHostPortValue) String() string {
	return "ConfigHostPortValue"
}

func (v ConfigHostPortValue) ToObjectValue(ctx context.Context) (basetypes.ObjectValue, diag.Diagnostics) {
	var diags diag.Diagnostics

	attributeTypes := map[string]attr.Type{}

	if v.IsNull() {
		return types.ObjectNull(attributeTypes), diags
	}

	if v.IsUnknown() {
		return types.ObjectUnknown(attributeTypes), diags
	}

	objVal, diags := types.ObjectValue(
		attributeTypes,
		map[string]attr.Value{})

	return objVal, diags
}

func (v ConfigHostPortValue) Equal(o attr.Value) bool {
	other, ok := o.(ConfigHostPortValue)

	if !ok {
		return false
	}

	if v.state != other.state {
		return false
	}

	if v.state != attr.ValueStateKnown {
		return true
	}

	return true
}

func (v ConfigHostPortValue) Type(ctx context.Context) attr.Type {
	return ConfigHostPortType{
		basetypes.ObjectType{
			AttrTypes: v.AttributeTypes(ctx),
		},
	}
}

func (v ConfigHostPortValue) AttributeTypes(ctx context.Context) map[string]attr.Type {
	return map[string]attr.Type{}
}
